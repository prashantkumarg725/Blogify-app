<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
  rel="stylesheet"
  integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
  crossorigin="anonymous"
/>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --primary-indigo: #6610f2;
    --primary-teal: #20c997;
    --light-gray: #f8f9fa;
    --dark-charcoal: #343a40;
    --white: #ffffff;
  }
  body {
    font-family: 'Roboto', sans-serif;
    background-color: #e9ecef;
    background-image: url("data:image/svg+xml,%3Csvg width='6' height='6' viewBox='0 0 6 6' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23d8dde2' fill-opacity='0.4' fill-rule='evenodd'%3E%3Cpath d='M5 0h1L0 6V5zM6 5v1H5z'/%3E%3C/g%3E%3C/svg%3E");
    color: var(--dark-charcoal);
  }
  h1, h2, h3, h4, h5, h6, .navbar-brand { font-family: 'Montserrat', sans-serif; font-weight: 700; }
  .navbar { background-color: rgba(255, 255, 255, 0.85); backdrop-filter: blur(10px); border-bottom: 1px solid rgba(0,0,0,0.1); }
  .navbar .nav-link, .navbar .navbar-brand { color: var(--dark-charcoal) !important; }
  .navbar .nav-link:hover { color: var(--primary-indigo) !important; }
  .hero-section { background: linear-gradient(45deg, var(--primary-indigo), var(--primary-teal)); color: var(--white); border-radius: 0.75rem; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }
  .card { border: none; background-color: var(--white); transition: transform 0.2s ease-in-out, box-shadow 0.3s ease-in-out; animation: fadeIn 0.5s ease-out forwards; opacity: 0; }
  .card:hover { transform: translateY(-10px); box-shadow: 0 16px 32px rgba(0,0,0,0.12); }
  .card-img-top { border-top-left-radius: calc(0.375rem - 1px); border-top-right-radius: calc(0.375rem - 1px); }
  .btn-primary { background-image: linear-gradient(45deg, var(--primary-indigo), var(--primary-teal)); border: none; transition: transform 0.2s, box-shadow 0.2s; background-size: 200% auto; }
  .btn-primary:hover { background-position: right center; transform: scale(1.05); box-shadow: 0 4px 15px rgba(102, 16, 242, 0.4); }
  .btn-ghost { background-color: transparent; border: 2px solid var(--white); color: var(--white); transition: all 0.3s ease; }
  .btn-ghost:hover { background-color: var(--white); color: var(--primary-indigo); transform: scale(1.05); }
  .blog-body { line-height: 1.8; font-size: 1.1rem; white-space: pre-wrap; }
  .footer { background-color: #2c3e50; color: var(--light-gray); }
</style>